# 公众号通知功能上线准备清单

更新时间：2026-07-17

## 一、当前已实现内容

目前系统已经具备“PC 派单后触发公众号通知”的后端链路。

已完成能力：

1. 小程序个人中心已有“绑定公众号通知”入口。
2. 后端已有运维人员公众号绑定表 `worker_wechat_binding`。
3. `user-service` 已提供绑定 URL、绑定状态、OAuth 回调接口。
4. `worker-order-service` 在 PC 派单成功后发布 RabbitMQ 事件 `work_order.assigned`。
5. `push-service` 监听 `work_order.assigned.queue`，根据 `worker_wechat_binding.official_open_id` 发送公众号通知。
6. `work_order` 表已有派单通知状态字段：
   - `dispatch_notify_status`
   - `dispatch_notify_message`
   - `dispatch_notified_at`

当前状态：

- `.env` 中 `WX_OFFICIAL_MOCK=true`
- 点击绑定公众号会走 mock 绑定。
- PC 派单后会走 mock 通知。
- 不会真实调用微信公众号接口。

## 二、真实上线前必须准备的条件

### 1. 公众号

需要准备一个真实公众号或可用于联调的测试公众号。

建议条件：

1. 公众号需要具备模板消息能力。
2. 公众号需要能获取 `AppID` 和 `AppSecret`。
3. 公众号后台需要能配置网页授权域名。
4. 如果后续采用“扫码关注公众号完成绑定”，还需要配置公众号服务器回调。

注意：

- 当前代码走的是公众号网页授权/OAuth 绑定链路。
- 公众号通知实际发送给的是公众号 `official_open_id`，不是小程序 `open_id`。
- 如果运维人员没有关注公众号，即使拿到了 openId，模板消息也可能无法正常发送。

### 2. 公众号模板消息

需要在公众号后台申请或选择一个“工单派单通知”模板。

当前代码按以下字段组装模板数据：

| 字段 | 当前含义 |
| --- | --- |
| `first` | 您有新的工单已派发 |
| `keyword1` | 工单编号 |
| `keyword2` | 运维人员姓名 |
| `keyword3` | 设备/地址 |
| `keyword4` | 派单时间 |
| `remark` | 请进入小程序查看工单详情并及时接单 |

因此模板需要包含类似字段。若微信后台模板字段不同，需要同步修改 `push-service` 中的模板字段映射。

需要准备：

- `WX_OFFICIAL_WORK_ORDER_ASSIGNED_TEMPLATE_ID`

### 3. 公网 HTTPS 域名

真实公众号 OAuth 回调不能使用内网 IP。

需要准备一个公网 HTTPS 域名，例如：

```text
https://api.example.com
```

需要确保该域名能访问 gateway，并能转发到：

```text
/api/v1/wechat/official/callback
```

正式回调地址示例：

```text
https://api.example.com/api/v1/wechat/official/callback
```

需要配置到：

```env
WX_OFFICIAL_OAUTH_CALLBACK_URL=https://api.example.com/api/v1/wechat/official/callback
```

### 4. 公众号网页授权域名

公众号后台需要配置网页授权域名。

配置域名应与 `WX_OFFICIAL_OAUTH_CALLBACK_URL` 所使用的域名一致。

示例：

```text
api.example.com
```

注意：

- 不要带 `http://` 或 `https://`。
- 不要带路径。
- 微信后台通常要求上传校验文件到域名根路径，部署时需要配合 Web/Nginx/Gateway 放置校验文件。

### 5. 小程序 web-view 业务域名

当前小程序通过 `web-view` 打开公众号绑定 URL。

如果真机或体验版要正常打开绑定页，需要在小程序后台配置业务域名。

需要准备：

1. 小程序 AppID。
2. 小程序业务域名。
3. HTTPS 证书。
4. 微信后台要求的校验文件。

当前配置项：

```env
WX_MINI_PROGRAM_APPID=wxb48a4a2e9ede4278
WX_WORK_ORDER_DETAIL_PAGE=pages/work-order/detail/index?id={orderId}
```

### 6. 后端环境变量

正式上线时需要将 `.env` 调整为真实配置。

必填项：

```env
WX_OFFICIAL_MOCK=false
WX_OFFICIAL_APPID=真实公众号AppID
WX_OFFICIAL_APPSECRET=真实公众号AppSecret
WX_OFFICIAL_OAUTH_CALLBACK_URL=https://你的域名/api/v1/wechat/official/callback
WX_OFFICIAL_WORK_ORDER_ASSIGNED_TEMPLATE_ID=公众号模板消息ID
WX_MINI_PROGRAM_APPID=真实小程序AppID
WX_WORK_ORDER_DETAIL_PAGE=pages/work-order/detail/index?id={orderId}
```

保留项：

```env
WX_OFFICIAL_BIND_TOKEN_EXPIRE_SECONDS=600
```

改完后至少需要重启：

```bash
docker compose up -d --no-deps user-service push-service
```

如果 gateway 的域名、HTTPS、反向代理配置有调整，也需要同步重启相关网关或 Nginx。

### 7. 数据库条件

当前库需要存在以下表和字段。

运维人员公众号绑定表：

```text
worker_wechat_binding
```

关键字段：

```text
worker_id
mini_open_id
official_open_id
subscribe_status
bound_at
deleted
```

工单表通知字段：

```text
work_order.dispatch_notify_status
work_order.dispatch_notify_message
work_order.dispatch_notified_at
```

通知发送前，运维人员需要满足：

```text
worker_wechat_binding.worker_id = 运维人员ID
worker_wechat_binding.official_open_id 不为空
worker_wechat_binding.subscribe_status = 1
worker_wechat_binding.deleted = 0
```

否则 PC 派单后通知状态会被标记为：

```text
SKIPPED
```

### 8. 运维人员绑定流程

上线前需要让运维人员完成公众号绑定。

当前流程：

1. 运维人员登录小程序。
2. 进入个人中心。
3. 点击“绑定公众号通知”。
4. 打开公众号 OAuth 授权页。
5. 授权回调后写入 `worker_wechat_binding`。

建议正式体验：

1. 如果运维人员未关注公众号，应提示先关注。
2. 更推荐后续升级为“参数二维码绑定”：
   - 小程序点击绑定。
   - 后端生成带 state 的公众号二维码。
   - 运维人员扫码关注公众号。
   - 公众号服务器收到 `subscribe/SCAN` 事件。
   - 后端完成 `worker_id` 与 `official_open_id` 绑定。

原因：

- 公众号模板消息通常要求用户已关注公众号。
- 扫码关注绑定比单纯 OAuth 更适合“通知类”场景。

## 三、可以提前测试的内容

### 1. 当前 mock 测试

不需要真实公众号。

可以测试：

1. 小程序入口是否显示。
2. 绑定状态接口是否正常。
3. mock 绑定是否写入 `worker_wechat_binding`。
4. PC 派单是否发布 `work_order.assigned` 事件。
5. `push-service` 是否消费事件。
6. `work_order.dispatch_notify_status` 是否回写为 `PUSHED`。

### 2. 真实 OAuth 测试

需要：

1. 真实或测试公众号。
2. 公网 HTTPS 回调域名。
3. 公众号网页授权域名配置。
4. `WX_OFFICIAL_MOCK=false`。

可以测试：

1. 点击绑定后是否跳转微信授权。
2. 回调是否进入 `/api/v1/wechat/official/callback`。
3. 是否拿到真实 `official_open_id`。
4. 是否写入 `worker_wechat_binding`。

### 3. 真实模板消息测试

需要：

1. 运维人员已关注公众号。
2. `worker_wechat_binding.subscribe_status=1`。
3. 已配置真实模板 ID。
4. 公众号 `AppID/AppSecret` 正确。
5. 服务器公网出口 IP 未被微信后台限制，或已加入公众号后台 IP 白名单。

可以测试：

1. PC 派单。
2. `push-service` 是否调用微信模板消息接口成功。
3. 运维人员微信是否收到公众号消息。
4. `work_order.dispatch_notify_status` 是否为 `PUSHED`。

## 四、上线前检查清单

账号与平台：

- [ ] 有真实或测试公众号。
- [ ] 公众号具备模板消息能力。
- [ ] 已获取公众号 `AppID`。
- [ ] 已获取公众号 `AppSecret`。
- [ ] 已准备小程序 AppID。
- [ ] 运维人员使用的小程序版本包含“绑定公众号通知”入口。

域名与 HTTPS：

- [ ] 有公网 HTTPS 域名。
- [ ] 域名能访问 gateway。
- [ ] `/api/v1/wechat/official/callback` 可公网访问。
- [ ] 公众号网页授权域名已配置。
- [ ] 小程序 web-view 业务域名已配置。
- [ ] 微信校验文件已能通过公网访问。

公众号配置：

- [ ] 已申请或选择派单通知模板。
- [ ] 模板字段与系统字段匹配。
- [ ] 已获得模板 ID。
- [ ] 如果公众号后台开启 IP 白名单，服务器出口 IP 已加入白名单。

系统配置：

- [ ] `WX_OFFICIAL_MOCK=false`
- [ ] `WX_OFFICIAL_APPID` 已配置。
- [ ] `WX_OFFICIAL_APPSECRET` 已配置。
- [ ] `WX_OFFICIAL_OAUTH_CALLBACK_URL` 已配置为 HTTPS 公网地址。
- [ ] `WX_OFFICIAL_WORK_ORDER_ASSIGNED_TEMPLATE_ID` 已配置。
- [ ] `WX_MINI_PROGRAM_APPID` 已配置。
- [ ] `WX_WORK_ORDER_DETAIL_PAGE` 已配置。
- [ ] `user-service` 已重启。
- [ ] `push-service` 已重启。

数据与人员：

- [ ] 至少一个运维人员已完成公众号绑定。
- [ ] `worker_wechat_binding.official_open_id` 不为空。
- [ ] `worker_wechat_binding.subscribe_status=1`。
- [ ] 运维人员已关注公众号。

测试验收：

- [ ] 小程序点击绑定能打开授权页。
- [ ] OAuth 回调成功。
- [ ] 绑定状态显示已绑定。
- [ ] PC 派单成功。
- [ ] `work_order.dispatch_notify_status=PUSHED`。
- [ ] 运维人员微信收到公众号通知。
- [ ] 未绑定运维人员派单时状态为 `SKIPPED`，不影响派单主流程。
- [ ] 微信接口异常时状态为 `FAILED`，不影响派单主流程。

## 五、当前主要风险点

1. 仅 OAuth 绑定不等于一定已关注公众号，真实通知前必须确认关注关系。
2. 公众号模板字段如果和代码字段不一致，会导致发送失败或内容不正确。
3. 回调域名、小程序 web-view 域名、公众号网页授权域名都需要微信后台校验。
4. 如果公众号后台设置了 IP 白名单，服务器出口 IP 变化会导致获取 `access_token` 失败。
5. 当前没有通知重试队列，发送失败只会标记 `FAILED`，后续可增加人工重发或自动重试。

## 六、建议后续优化

1. 增加“参数二维码关注绑定”流程，确保绑定时完成关注。
2. PC 工单详情页显示派单通知状态和失败原因。
3. 增加通知失败重试接口。
4. 增加推送记录表，保存每次通知请求、响应和错误码。
5. 针对模板消息字段做配置化，避免更换模板时必须改代码。
