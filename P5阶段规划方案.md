# P5 阶段规划方案 - Vue 3 管理后台

> **编制日期**：2026-07-08
> **基于**：直饮水平台详细设计文档 V3.0 第七章
> **目标**：交付完整可运行的 Vue 3 管理后台（PC 端）
> **预计页面数**：40+ 个页面，覆盖 13 个业务模块

---

## 一、总体目标

### 1.1 交付物清单

| 类型 | 内容 | 数量 |
|------|------|------|
| 页面 | 全部业务页面（含列表/详情/表单/统计） | 40+ |
| 通用组件 | Layout / 表格 / 表单 / 图表 / 弹窗 / 导入 等 | 15+ |
| 业务组件 | 各模块的复杂业务组件 | 25+ |
| Pinia Store | 用户 / 权限 / 字典 / 业务 | 8+ |
| API 封装 | 16 个微服务的 axios 请求层 | 16 个模块 |
| 工具函数 | request / token / format / validate | 10+ |
| 部署产物 | Docker 镜像 + Nginx 配置 + 路由代理 | 1 套 |

### 1.2 验收标准

- [ ] 全部 40+ 页面可正常访问
- [ ] 16 个微服务 API 全部对接（含 JWT 注入、错误处理、Loading）
- [ ] 13 个业务模块的 CRUD/查询/统计功能可用
- [ ] 仪表盘图表渲染（5+ 种图表：柱状/折线/饼图/卡片/进度条）
- [ ] 路由守卫、权限按钮、动态菜单全部生效
- [ ] Docker 镜像构建 + Nginx 部署验证通过
- [ ] 与后端 37+ 端点的端到端联调通过

---

## 二、技术栈选型

### 2.1 核心技术栈

| 层级 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 框架 | Vue 3 | 3.4+ | Composition API + `<script setup>` |
| 语言 | TypeScript | 5.4+ | 类型安全 |
| 构建 | Vite | 5.2+ | 快速 HMR + 构建 |
| UI 库 | Element Plus | 2.7+ | 后台组件库 |
| 状态管理 | Pinia | 2.1+ | 替代 Vuex，TypeScript 友好 |
| 路由 | Vue Router | 4.3+ | 嵌套路由 + 守卫 |
| HTTP | Axios | 1.6+ | 请求拦截 + Token 注入 |
| 图表 | ECharts | 5.5+ | 仪表盘大屏 |
| 工具 | VueUse | 10+ | 实用 Composition 工具集 |
| 工具 | dayjs | 1.11+ | 轻量日期库 |
| 工具 | nprogress | 0.2+ | 路由进度条 |
| 工具 | js-cookie | 3.0+ | Token 持久化 |
| Excel | xlsx | 0.18+ | 批量导入导出 |
| CSS | SCSS | 1.77+ | 嵌套 + 变量 |

### 2.2 选型理由

- **Vue 3 + TS**：与设计文档一致；TypeScript 提升大型后台可维护性
- **Vite 5**：冷启动 < 1s，HMR < 100ms，优于 Webpack
- **Element Plus**：Vue 3 生态最成熟的中后台组件库
- **Pinia**：Vue 官方推荐，组合式 API 友好，比 Vuex 更轻量
- **ECharts**：百度开源，国内最主流，文档/示例丰富
- **Axios**：拦截器机制完善，TypeScript 类型支持好

### 2.3 不引入的依赖

- ❌ **Vuex**：已被 Pinia 取代
- ❌ **jQuery**：Vue 时代无需
- ❌ **Lodash**：用 ES 原生方法 + VueUse 替代（避免打包体积膨胀）
- ❌ **Mock.js**：用后端真实 API（37+ 端点已就绪）
- ❌ **Ant Design Vue**：与 Element Plus 冲突，选其一

---

## 三、项目结构

### 3.1 目录树

```
drinking-water-admin/
├── public/
│   ├── favicon.ico
│   └── logo.svg
├── src/
│   ├── api/                      # API 封装层（按微服务划分）
│   │   ├── auth.ts
│   │   ├── user.ts               # user-service（customers/dealers/workers/sys-users）
│   │   ├── device.ts             # device-service（device-models/devices）
│   │   ├── iot.ts                # iot-service（commands/online-status）
│   │   ├── filter.ts             # filter-service（filter-models/filters）
│   │   ├── workOrder.ts          # worker-order-service
│   │   ├── package.ts            # package-service
│   │   ├── order.ts              # order-service
│   │   ├── payment.ts            # payment-service
│   │   ├── finance.ts            # finance-service
│   │   ├── inventory.ts          # inventory-service
│   │   ├── monitor.ts            # monitor-service
│   │   ├── report.ts             # report-service
│   │   ├── system.ts             # system-service
│   │   ├── push.ts               # push-service
│   │   └── index.ts              # 统一导出
│   ├── assets/                   # 静态资源
│   │   ├── images/
│   │   └── icons/
│   ├── components/               # 通用组件
│   │   ├── Layout/
│   │   │   ├── AdminLayout.vue
│   │   │   ├── Sidebar.vue
│   │   │   ├── Header.vue
│   │   │   ├── Breadcrumb.vue
│   │   │   └── TagsView.vue          # 多页签（可选）
│   │   ├── Table/
│   │   │   ├── PageTable.vue         # 通用分页表格（含搜索/批量操作）
│   │   │   └── index.ts
│   │   ├── Form/
│   │   │   ├── FormDialog.vue        # 通用表单弹窗
│   │   │   └── index.ts
│   │   ├── Chart/
│   │   │   ├── BarChart.vue
│   │   │   ├── LineChart.vue
│   │   │   ├── PieChart.vue
│   │   │   ├── GaugeChart.vue
│   │   │   └── index.ts
│   │   ├── Upload/
│   │   │   ├── FileUpload.vue        # 通用文件上传
│   │   │   └── BatchImport.vue       # Excel 批量导入
│   │   ├── Business/
│   │   │   ├── StatusTag.vue         # 状态标签（订单/工单/支付等）
│   │   │   ├── AmountCell.vue        # 金额单元格（分转元）
│   │   │   └── DateRangeFilter.vue   # 时间范围筛选
│   │   └── index.ts
│   ├── composables/              # 组合式函数
│   │   ├── useTable.ts              # 表格逻辑封装
│   │   ├── useDict.ts               # 字典数据
│   │   ├── usePermission.ts         # 权限指令
│   │   └── useDownload.ts           # 文件下载
│   ├── directives/               # 自定义指令
│   │   ├── permission.ts            # v-permission="'user:add'"
│   │   └── copy.ts                  # v-copy
│   ├── layouts/                  # 布局组件
│   │   └── AdminLayout.vue          # 顶层布局（含路由出口）
│   ├── router/
│   │   ├── index.ts
│   │   ├── routes.ts                # 静态路由
│   │   ├── modules/                 # 模块路由（按业务拆分）
│   │   │   ├── dashboard.ts
│   │   │   ├── device.ts
│   │   │   ├── filter.ts
│   │   │   ├── customer.ts
│   │   │   ├── dealer.ts
│   │   │   ├── worker.ts
│   │   │   ├── workOrder.ts
│   │   │   ├── order.ts
│   │   │   ├── package.ts
│   │   │   ├── inventory.ts
│   │   │   ├── finance.ts
│   │   │   ├── report.ts
│   │   │   ├── system.ts
│   │   │   └── monitor.ts
│   │   └── guards.ts                # 全局守卫
│   ├── stores/                   # Pinia 状态
│   │   ├── user.ts                  # 用户信息/token
│   │   ├── permission.ts            # 动态路由/权限
│   │   ├── dict.ts                  # 字典
│   │   ├── app.ts                   # 全局 UI 状态
│   │   └── tags.ts                  # 多页签
│   ├── styles/                   # 全局样式
│   │   ├── index.scss
│   │   ├── variables.scss
│   │   ├── reset.scss
│   │   └── element-plus.scss        # 主题定制
│   ├── types/                    # 类型定义
│   │   ├── api.d.ts                 # 后端 VO/DTO 类型
│   │   ├── global.d.ts
│   │   ├── router.d.ts
│   │   └── store.d.ts
│   ├── utils/                    # 工具函数
│   │   ├── request.ts               # Axios 实例 + 拦截器
│   │   ├── auth.ts                  # token 存取
│   │   ├── format.ts                # 金额/日期格式化
│   │   ├── validate.ts              # 校验函数
│   │   ├── dict.ts                  # 字典工具
│   │   └── download.ts              # 文件下载
│   ├── views/                    # 页面（按业务模块）
│   │   ├── login/
│   │   │   └── index.vue
│   │   ├── dashboard/
│   │   │   └── index.vue
│   │   ├── device/
│   │   │   ├── list.vue
│   │   │   ├── register.vue
│   │   │   ├── detail.vue
│   │   │   └── components/
│   │   │       ├── DeviceTable.vue
│   │   │       └── DeviceRegisterForm.vue
│   │   ├── filter/
│   │   │   ├── list.vue
│   │   │   ├── inbound.vue
│   │   │   ├── trace.vue
│   │   │   ├── stats.vue
│   │   │   └── components/
│   │   ├── customer/
│   │   │   ├── list.vue
│   │   │   ├── detail.vue
│   │   │   └── components/
│   │   ├── dealer/
│   │   │   ├── list.vue
│   │   │   └── components/
│   │   ├── worker/
│   │   │   ├── list.vue
│   │   │   └── components/
│   │   ├── workOrder/
│   │   │   ├── list.vue
│   │   │   ├── detail.vue
│   │   │   └── components/
│   │   ├── order/
│   │   │   ├── list.vue
│   │   │   ├── abnormal.vue
│   │   │   └── components/
│   │   ├── package/
│   │   │   ├── list.vue
│   │   │   └── components/
│   │   ├── inventory/
│   │   │   ├── devices.vue
│   │   │   ├── filters.vue
│   │   │   ├── batches.vue
│   │   │   └── components/
│   │   ├── finance/
│   │   │   ├── records.vue
│   │   │   ├── settlements.vue
│   │   │   ├── commissions.vue
│   │   │   ├── invoices.vue
│   │   │   ├── refunds.vue
│   │   │   └── components/
│   │   ├── report/
│   │   │   ├── waterQuality.vue
│   │   │   ├── flow.vue
│   │   │   ├── revenue.vue
│   │   │   ├── workOrder.vue
│   │   │   └── components/
│   │   ├── system/
│   │   │   ├── roles.vue
│   │   │   ├── users.vue
│   │   │   ├── configs.vue
│   │   │   ├── auditLogs.vue
│   │   │   └── components/
│   │   ├── monitor/
│   │   │   ├── alerts.vue
│   │   │   ├── thresholds.vue
│   │   │   └── components/
│   │   └── error/
│   │       ├── 404.vue
│   │       └── 403.vue
│   ├── App.vue
│   ├── main.ts
│   └── env.d.ts
├── .env.development              # 开发环境
├── .env.production               # 生产环境
├── .eslintrc.cjs
├── .prettierrc
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
├── Dockerfile
├── nginx.conf                    # 生产 Nginx 配置
├── README.md
└── docker-compose-admin.yml      # 独立部署的 compose
```

### 3.2 关键目录说明

- **api/**：16 个微服务各一个 ts 文件，与后端 Java 包结构一一对应
- **views/**：按业务模块组织，每个模块一个文件夹（device/filter/order/...）
- **components/**：通用组件，业务无关；业务组件放 views/xxx/components/
- **router/modules/**：每个业务模块一个路由配置文件，主路由动态加载
- **stores/**：Pinia 替代 Vuex，按职责拆分 user/permission/dict/app/tags
- **types/api.d.ts**：根据后端 VO 类手写 TS 接口（无 OpenAPI 工具时）

---

## 四、核心模块设计

### 4.1 路由与权限（router/）

#### 静态路由
```typescript
// router/routes.ts
export const constantRoutes = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/login/index.vue'),
    meta: { hidden: true }
  },
  {
    path: '/404',
    name: 'NotFound',
    component: () => import('@/views/error/404.vue'),
    meta: { hidden: true }
  },
  {
    path: '/',
    component: AdminLayout,
    redirect: '/dashboard',
    children: [
      { path: 'dashboard', name: 'Dashboard', component: () => import('@/views/dashboard/index.vue'), meta: { title: '仪表盘', icon: 'Odometer' } }
      // ... 动态注入的业务路由
    ]
  },
  { path: '/:pathMatch(.*)*', redirect: '/404' }
]
```

#### 动态路由（基于权限）
```typescript
// router/modules/device.ts
export default {
  path: '/devices',
  component: AdminLayout,
  meta: { title: '设备管理', icon: 'Box', roles: ['admin', 'operator'] },
  children: [
    { path: 'list', name: 'DeviceList', component: () => import('@/views/device/list.vue'), meta: { title: '设备列表', permission: 'device:view' } },
    { path: 'register', name: 'DeviceRegister', component: () => import('@/views/device/register.vue'), meta: { title: '设备登记', permission: 'device:add' } },
    { path: 'detail/:deviceId', name: 'DeviceDetail', component: () => import('@/views/device/detail.vue'), meta: { title: '设备详情', hidden: true, activeMenu: '/devices/list' } }
  ]
}
```

#### 全局守卫
```typescript
// router/guards.ts
router.beforeEach(async (to, from, next) => {
  nprogress.start()
  const userStore = useUserStore()
  const token = getToken()
  if (!token) {
    to.path === '/login' ? next() : next(`/login?redirect=${to.path}`)
    return
  }
  // 已登录
  if (!userStore.userInfo) {
    await userStore.fetchUserInfo()
    const accessRoutes = await usePermissionStore().buildRoutes()
    accessRoutes.forEach(r => router.addRoute(r))
    next({ ...to, replace: true })
    return
  }
  next()
})
```

### 4.2 API 封装（api/ + utils/request.ts）

#### Axios 实例
```typescript
// utils/request.ts
const service = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,  // http://localhost:8080/api
  timeout: 30000
})

// 请求拦截：注入 token
service.interceptors.request.use(config => {
  const token = getToken()
  if (token) config.headers.Authorization = `Bearer ${token}`
  return config
})

// 响应拦截：统一错误处理
service.interceptors.response.use(
  response => {
    const res = response.data
    if (res.code === 200) return res
    // 业务错误码
    ElMessage.error(res.message || 'Error')
    if (res.code === 40100) {
      // token 失效
      useUserStore().logout()
      router.push('/login')
    }
    return Promise.reject(new Error(res.message))
  },
  error => {
    ElMessage.error(error.message || 'Network Error')
    return Promise.reject(error)
  }
)
```

#### API 模块示例
```typescript
// api/device.ts
import request from '@/utils/request'
import type { PageQueryDTO, DeviceVO, PageResult, DeviceRegisterDTO } from '@/types/api'

export const pageDevices = (data: PageQueryDTO & { keyword?: string; status?: string }) =>
  request.get<PageResult<DeviceVO>, PageResult<DeviceVO>>('/device-service/devices', { params: data })

export const registerDevice = (data: DeviceRegisterDTO) =>
  request.post<DeviceVO, DeviceVO>('/device-service/devices', data)

export const getDevice = (deviceId: string) =>
  request.get<DeviceVO, DeviceVO>(`/device-service/devices/${deviceId}`)

export const updateDevice = (deviceId: string, data: Partial<DeviceVO>) =>
  request.put<void, void>(`/device-service/devices/${deviceId}`, data)
```

#### 路径约定
- 路径前缀：每个 API 文件头部定义服务前缀常量
  - auth.ts → `/auth-service`
  - device.ts → `/device-service`
  - iot.ts → `/iot-service`
- 最终 URL：`http://gateway:8080/api/{service-name}/{endpoint}`
- 由 Gateway 路由到具体微服务

### 4.3 Pinia Store（stores/）

#### user.ts
```typescript
export const useUserStore = defineStore('user', {
  state: () => ({
    token: getToken() || '',
    userInfo: null as SysUserVO | null,
    permissions: [] as string[],
    roles: [] as string[]
  }),
  actions: {
    async login(account: string, password: string) {
      const res = await loginApi({ account, password })
      this.token = res.data.accessToken
      this.userInfo = res.data.userInfo
      this.permissions = res.data.permissions
      this.roles = res.data.roles
      setToken(this.token)
    },
    async fetchUserInfo() {
      const res = await getCurrentUser()
      this.userInfo = res.data
    },
    async logout() {
      await logoutApi()
      this.resetState()
      removeToken()
    }
  }
})
```

#### permission.ts
```typescript
export const usePermissionStore = defineStore('permission', {
  state: () => ({
    routes: [] as RouteRecordRaw[],
    addRoutes: [] as RouteRecordRaw[]
  }),
  actions: {
    async buildRoutes() {
      // 根据 roles 过滤动态路由
      const accessedRoutes = asyncRoutes.filter(r => {
        if (r.meta?.roles) return r.meta.roles.some(role => userStore.roles.includes(role))
        return true
      })
      this.addRoutes = accessedRoutes
      return accessedRoutes
    }
  }
})
```

### 4.4 通用组件（components/）

#### PageTable（核心）
```vue
<!-- components/Table/PageTable.vue -->
<template>
  <div class="page-table">
    <!-- 搜索区 -->
    <div class="search-bar" v-if="searchFields.length">
      <el-form :model="searchForm" inline @submit.prevent="handleSearch">
        <el-form-item v-for="f in searchFields" :key="f.prop" :label="f.label">
          <component
            :is="f.component"
            v-model="searchForm[f.prop]"
            v-bind="f.props"
          />
        </el-form-item>
        <el-form-item>
          <el-button type="primary" @click="handleSearch">搜索</el-button>
          <el-button @click="handleReset">重置</el-button>
        </el-form-item>
      </el-form>
    </div>

    <!-- 工具栏 -->
    <div class="toolbar">
      <slot name="toolbar-left">
        <el-button v-permission="addPermission" type="primary" @click="$emit('add')">
          <el-icon><Plus /></el-icon>新增
        </el-button>
      </slot>
      <slot name="toolbar-right">
        <el-button @click="loadData"><el-icon><Refresh /></el-icon></el-button>
      </slot>
    </div>

    <!-- 表格 -->
    <el-table v-loading="loading" :data="dataList" border stripe>
      <el-table-column v-if="selection" type="selection" width="50" />
      <el-table-column v-for="col in columns" :key="col.prop"
        :prop="col.prop" :label="col.label" :width="col.width"
        :formatter="col.formatter">
        <template #default="scope" v-if="col.slot">
          <slot :name="col.slot" :row="scope.row" :$index="scope.$index" />
        </template>
      </el-table-column>
      <el-table-column label="操作" width="180" fixed="right">
        <template #default="scope">
          <slot name="actions" :row="scope.row" />
        </template>
      </el-table-column>
    </el-table>

    <!-- 分页 -->
    <el-pagination
      v-model:current-page="pagination.pageNum"
      v-model:page-size="pagination.pageSize"
      :total="pagination.total"
      :page-sizes="[10, 20, 50, 100]"
      layout="total, sizes, prev, pager, next, jumper"
      @current-change="loadData"
      @size-change="loadData"
    />
  </div>
</template>
```

#### DashboardCharts（仪表盘核心）
```vue
<!-- views/dashboard/components/DashboardCharts.vue -->
<template>
  <el-row :gutter="20">
    <el-col :span="6" v-for="card in cards" :key="card.title">
      <el-card class="stat-card">
        <div class="stat-title">{{ card.title }}</div>
        <div class="stat-value" :style="{ color: card.color }">
          {{ card.value }}
        </div>
        <div class="stat-trend">较昨日 {{ card.trend }}</div>
      </el-card>
    </el-col>
  </el-row>

  <el-row :gutter="20" style="margin-top: 20px">
    <el-col :span="12">
      <el-card title="设备在线趋势">
        <LineChart :option="onlineTrendOption" />
      </el-card>
    </el-col>
    <el-col :span="12">
      <el-card title="营收分布">
        <PieChart :option="revenuePieOption" />
      </el-card>
    </el-col>
  </el-row>
</template>
```

### 4.5 13 个业务模块清单

按设计文档第七章 7.1 节路由设计：

| # | 路由 | 页面 | 主要功能 | 对接微服务 |
|---|------|------|----------|-----------|
| 1 | /login | 登录页 | 账号密码 + 图形验证码 | auth-service |
| 2 | /dashboard | 仪表盘 | 5+ 统计卡片 + 5+ 图表 | report-service |
| 3 | /devices/list | 设备列表 | 分页/筛选/导出 | device-service |
| 4 | /devices/register | 设备登记 | 表单 + QR 码生成 | device-service |
| 5 | /devices/detail/:id | 设备详情 | 基本信息/状态/滤芯/工单 | device + filter + workOrder |
| 6 | /filters/list | 滤芯列表 | 分页/筛选 | filter-service |
| 7 | /filters/inbound | 滤芯入库 | 批量/单个 | filter-service |
| 8 | /filters/trace/:id | 滤芯追溯 | 时间线/寿命 | filter + iot |
| 9 | /filters/stats | 滤芯统计 | 图表 | filter + report |
| 10 | /customers | 客户列表 | CRUD | user-service |
| 11 | /customers/detail/:id | 客户详情 | 设备/订单/工单 | user + device + order |
| 12 | /dealers | 经销商管理 | 树形结构/分润规则 | user-service |
| 13 | /workers | 运维人员 | CRUD + 技能 | user-service |
| 14 | /work-orders | 工单管理 | 列表/派单/跟踪 | worker-order-service |
| 15 | /orders | 订单管理 | 列表/详情/取消 | order-service |
| 16 | /orders/abnormal | 异常订单 | 筛选 | order-service |
| 17 | /packages | 套餐管理 | CRUD/上下架 | package-service |
| 18 | /inventory/devices | 设备库存 | 列表/调拨/入库 | inventory-service |
| 19 | /inventory/filters | 滤芯库存 | 列表 | inventory-service |
| 20 | /inventory/batches | 进货批次 | 列表 | inventory-service |
| 21 | /finance/records | 充值记录 | 列表 | payment-service |
| 22 | /finance/settlements | 结算账单 | 列表/结算 | finance-service |
| 23 | /finance/commissions | 分佣明细 | 列表 | finance-service |
| 24 | /finance/invoices | 发票管理 | 列表/开票 | finance-service |
| 25 | /finance/refunds | 退款管理 | 列表/审批 | payment-service |
| 26 | /reports/water-quality | 水质报表 | 折线图 | report + iot |
| 27 | /reports/flow | 流量报表 | 柱状图 | report + iot |
| 28 | /reports/revenue | 营收报表 | 图表 | report + finance |
| 29 | /reports/work-order | 工单报表 | 图表 | report + workOrder |
| 30 | /system/roles | 角色管理 | CRUD | system-service |
| 31 | /system/users | 账户管理 | CRUD/重置密码 | system-service |
| 32 | /system/configs | 系统配置 | 列表/编辑 | system-service |
| 33 | /system/audit-logs | 审计日志 | 列表 | system-service |
| 34 | /monitor/alerts | 告警列表 | 处理/筛选 | monitor-service |
| 35 | /monitor/thresholds | 阈值管理 | 编辑 | monitor-service |
| 36 | /404 + /403 | 错误页 | 静态 | - |

**合计：36+ 个页面**

---

## 五、关键功能设计

### 5.1 登录流程

```
用户输入账号密码
  → POST /auth-service/auth/login
  → 后端返回 { accessToken, refreshToken, userInfo, permissions, roles }
  → Pinia 持久化（localStorage + cookie）
  → 路由守卫放行，跳转 /dashboard
```

### 5.2 权限控制（三级）

1. **路由级**：动态路由根据 roles 过滤
2. **菜单级**：侧边栏菜单按权限渲染
3. **按钮级**：v-permission 指令控制按钮显隐

```vue
<el-button v-permission="['device:add']" @click="onAdd">新增设备</el-button>
```

### 5.3 列表页标准模式（CRUD）

```
PageTable 通用组件
  → 配置 searchFields（搜索表单）
  → 配置 columns（表格列）
  → 插槽 actions（操作按钮：编辑/删除/详情）
  → 父组件传 fetchApi + deleteApi
```

### 5.4 表单页标准模式

- 使用 Element Plus Form
- 统一 FormDialog 弹窗组件
- 校验规则：必填、长度、类型、自定义
- 提交后：关闭弹窗 + 刷新列表 + 提示

### 5.5 仪表盘图表选型

| 指标 | 图表 | 数据源 |
|------|------|--------|
| 设备总数/在线/离线 | 数字卡片 | report-service /reports/dashboard |
| 总营收/订单 | 数字卡片 | report-service |
| 设备在线趋势（30天） | 折线图 | report-service /reports/devices |
| 订单状态分布 | 饼图 | report-service /reports/orders |
| 营收按月 | 柱状图 | report-service /reports/finance |
| 工单状态分布 | 环形图 | report-service /reports/workers |
| 告警等级分布 | 漏斗图 | monitor-service |
| 滤芯寿命 TOP10 | 条形图 | filter-service |

### 5.6 错误处理

- **网络错误**：Axios 拦截器统一提示
- **业务错误码**：后端约定 200=成功，其他均视为失败，弹窗提示 message
- **40100 (token 失效)**：自动登出 + 跳转登录
- **404**：跳转 /404
- **403**：跳转 /403
- **500**：提示"服务器内部错误"

### 5.7 性能优化

- 路由懒加载：`() => import('@/views/...')`
- 表格虚拟滚动：el-table-v2（大数据量）
- 图表按需引入 ECharts 模块
- 列表防抖搜索
- 图片懒加载
- 路由 keep-alive（多页签）

---

## 六、UI 设计规范

### 6.1 主题色

```scss
// variables.scss
$primary: #1890ff;        // 主色（与 Element Plus 默认一致）
$success: #52c41a;
$warning: #faad14;
$danger: #ff4d4f;
$info: #909399;

$header-height: 60px;
$sidebar-width: 220px;
$sidebar-collapsed-width: 64px;
```

### 6.2 布局

```
┌─────────────────────────────────────────────┐
│              Header (logo + 面包屑 + 用户)        │
├──────────┬──────────────────────────────────┤
│          │                                  │
│ Sidebar  │        Main Content              │
│ (菜单)     │        (路由出口)                  │
│          │                                  │
│          │                                  │
└──────────┴──────────────────────────────────┘
```

### 6.3 列表页标准布局

```
┌─ 搜索栏 (折叠展开) ─────────────┐
│ [设备ID] [状态] [时间] [搜索] [重置]   │
└────────────────────────────────┘
┌─ 工具栏 ─────────────────────┐
│ [+ 新增]  [批量删除]   [导出]  [刷新]   │
└────────────────────────────────┘
┌─ 表格 (分页) ──────────────────┐
│  ID  设备ID  型号  状态  ...  操作   │
│  1   D001   X100  在线    ...  编辑/删除 │
│  ...                             │
└────────────────────────────────┘
[共 100 条] [10/页] [< 1 2 3 ... >]
```

---

## 七、API 对接清单（37+ 端点）

### 7.1 P0-P4 阶段已验证的 37 个端点，全部需要前端对接

#### auth (4 端点)
- POST /auth/login
- POST /auth/refresh
- POST /auth/logout
- GET /auth/current-user

#### user (8 端点)
- GET/POST/PUT/DELETE /customers + /{id}
- GET/POST/PUT/DELETE /dealers + /{id}
- GET/POST/PUT/DELETE /workers + /{id}
- GET/POST/PUT/DELETE /sys-users + /{id}

#### device (5 端点)
- GET/POST/PUT/DELETE /device-models + /{id}
- GET/POST /devices
- GET/PUT/DELETE /devices/{id}

#### iot (3 端点)
- GET /iot/devices/{sn}/latest
- POST /iot/devices/{sn}/command
- GET /iot/devices/{sn}/online-status

#### filter (5 端点)
- GET/POST/PUT/DELETE /filter-models
- GET/POST /filters + /filters/register

#### work-order (4 端点)
- GET/POST /work-orders
- PUT /work-orders/{id}/dispatch
- GET /work-orders/statistics

#### package (4 端点)
- GET/POST/PUT /packages
- PUT /packages/{id}/online + /offline

#### order (5 端点)
- GET/POST /orders
- PUT /orders/{no}/pay + /cancel
- GET /orders/statistics

#### payment (5 端点)
- GET/POST /payments
- GET/POST /payments/refunds
- PUT /payments/refunds/{no}/approve + /reject

#### finance (6 端点)
- GET /finance/summary
- GET/POST /finance/settlements
- GET/finance/commissions
- GET/POST /finance/invoices

#### inventory (5 端点)
- GET/POST /inventory/devices + /inbound + /transfer
- GET/POST /inventory/batches

#### monitor (5 端点)
- GET /monitor/alerts + /statistics
- GET/PUT /monitor/thresholds
- PUT /monitor/alerts/{id}/handle

#### report (5 端点)
- GET /reports/dashboard
- GET /reports/devices + /orders + /finance + /workers

#### system (4 端点)
- GET/PUT /system/configs
- GET /system/audit-logs
- (sys-users 在 user-service)

#### push (3 端点)
- GET /push/unpushed
- POST /push/alerts/{id}
- GET /push/stats

**合计：71+ 端点（含本次未单独列出的 PUT/DELETE）**

---

## 八、部署方案

### 8.1 Docker 镜像构建

```dockerfile
# 多阶段构建
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:1.25-alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

### 8.2 Nginx 配置

```nginx
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # SPA 路由
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 反向代理到后端 Gateway
    location /api/ {
        proxy_pass http://gateway:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

### 8.3 docker-compose 集成

在 `docker-compose.yml` 中新增 admin 容器：

```yaml
admin-web:
  build: ./admin-web
  container_name: drinking-water-admin-web
  ports:
    - "8088:80"
  depends_on:
    - gateway
  networks:
    - drinking-water-net
  restart: unless-stopped
```

访问：`http://localhost:8088`

### 8.4 开发环境

```bash
# 安装依赖
npm install

# 启动开发服务器（Vite）
npm run dev
# 访问 http://localhost:5173
# 自动代理 /api 到 http://localhost:8080

# 类型检查
npm run type-check

# 代码检查
npm run lint

# 构建生产
npm run build
```

---

## 九、开发计划

### 9.1 工作分解（WBS）

| 阶段 | 任务 | 工作量估算 | 产出 |
|------|------|-----------|------|
| **P5.1 基础设施** | Vite 项目初始化、TS 配置、ESLint、目录结构、Axios、Pinia、路由骨架 | 1 天 | 可启动的空项目 |
| **P5.2 通用组件** | Layout / PageTable / FormDialog / Chart / Upload / 权限指令 | 1.5 天 | 15+ 通用组件 |
| **P5.3 API 封装** | 16 个微服务的 API 封装 + 类型定义 | 0.5 天 | 71+ 端点 |
| **P5.4 登录 + 权限** | 登录页、路由守卫、动态菜单、按钮权限 | 0.5 天 | 完整登录流程 |
| **P5.5 仪表盘** | 9 个图表 + 统计卡片 | 0.5 天 | 1 个完整页面 |
| **P5.6 业务模块** | 13 个业务模块（设备/滤芯/客户/工单/订单/...） | 3-4 天 | 35+ 业务页面 |
| **P5.7 联调 + 修复** | 与 16 个微服务联调、修复 bug | 1 天 | 端到端验证通过 |
| **P5.8 部署** | Docker 镜像、Nginx 部署、生产环境验证 | 0.5 天 | 生产可访问 |
| **合计** | | **8-9 天** | **36+ 页面** |

### 9.2 优先级

- **P0 (核心)**: 登录、Layout、PageTable、Dashboard、设备管理（list/register/detail）
- **P1 (重要)**: 滤芯、客户、经销商、工人、工单、订单
- **P2 (次要)**: 套餐、库存、财务、监控、报表、系统配置

### 9.3 风险点

| 风险 | 影响 | 应对 |
|------|------|------|
| 后端接口字段变更 | 中 | API 层集中封装，易于适配 |
| 大量 CRUD 页面工作量大 | 中 | PageTable 通用组件 + 模板代码 |
| 权限模型复杂 | 低 | 当前仅 admin/operator 两角色，按 v-permission 控制即可 |
| ECharts 打包体积大 | 中 | 按需引入 echarts/core + 必要 chart 类型 |
| 后端无 OpenAPI 文档 | 中 | 直接读后端 VO Java 类写 TS 类型 |

---

## 十、配置清单

### 10.1 环境变量

```bash
# .env.development
VITE_API_BASE_URL=http://localhost:8080/api
VITE_APP_TITLE=直饮水平台管理后台
VITE_USE_MOCK=false

# .env.production
VITE_API_BASE_URL=/api
VITE_APP_TITLE=直饮水平台管理后台
```

### 10.2 Vite 关键配置

```typescript
// vite.config.ts
export default defineConfig({
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  },
  build: {
    target: 'es2015',
    chunkSizeWarningLimit: 1500,
    rollupOptions: {
      output: {
        manualChunks: {
          'vue-vendor': ['vue', 'vue-router', 'pinia'],
          'element-plus': ['element-plus', '@element-plus/icons-vue'],
          'echarts': ['echarts']
        }
      }
    }
  }
})
```

### 10.3 tsconfig.json 关键配置

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "jsx": "preserve",
    "sourceMap": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
```

---

## 十一、与后端联调策略

### 11.1 联调顺序

1. **P5.1 完成后**：启动后端所有微服务 + 前端开发服务器
2. **P5.4 登录后**：打通 token 流程，验证权限控制
3. **每个业务模块完成后**：联调对应微服务的所有端点
4. **P5.7 全量联调**：所有页面、所有按钮、所有操作

### 11.2 接口联调 Checklist

每个 API 封装完成后，验证：
- [ ] 请求 URL 正确
- [ ] 请求方法正确（GET/POST/PUT/DELETE）
- [ ] 请求参数格式正确（query/body/path）
- [ ] 响应数据反序列化无错
- [ ] 业务错误码处理
- [ ] Loading 状态
- [ ] 空数据/边界条件

### 11.3 已知后端问题规避

- **PageResult 字段**：`total`/`size`/`current`/`records`（非 pages/pageSize）
- **设备状态**：使用 `lifecycle_status` 字段（REGISTERED/ALLOCATED/...）
- **订单金额**：`pay_amount` 字段（分）
- **逻辑删除**：列表查询需过滤 `deleted = 0`（MyBatis-Plus 自动处理）
- **金额单位**：分，前端展示需除以 100，保留 2 位小数

---

## 十二、交付物清单

| 类别 | 内容 | 数量 |
|------|------|------|
| 源代码 | Vue 3 + TS 完整项目 | 1 套 |
| 配置文件 | vite/ts/eslint/prettier/scss | 6 个 |
| 部署 | Dockerfile + nginx.conf + compose 集成 | 3 个 |
| 文档 | README + 本规划方案 | 2 份 |
| 验证 | 36+ 页面截图 + 联调报告 | 1 份 |
| 镜像 | drinking-water-admin-web:latest | 1 个 |

---

## 十三、确认事项

**P5 启动前需要用户确认**：

1. ✅ **TypeScript 是否启用**？（推荐启用）
2. ✅ **项目路径**：`C:\Users\Administrator\Desktop\直饮水平台开发\admin-web`
3. ✅ **包管理器**：npm（默认）/ pnpm（更快）
4. ✅ **Node 版本**：20 LTS
5. ✅ **ECharts 还是 Chart.js**？（推荐 ECharts，国内生态成熟）
6. ✅ **是否需要多页签 (Tags-View)**？（可选功能）
7. ✅ **是否需要暗色主题切换**？（设计文档未要求，可选）
8. ✅ **国际化 (i18n)** 是否需要？（设计文档未要求，默认中文）

---

**P5 阶段规划完毕。等待用户确认后开始执行。**
