# IC 卡刷卡取水实现路径图

## 设计边界

- 卡类型：普通 `MIFARE Classic M1` 卡。
- 卡内不保存余额；余额、扣费、退款和订单均以平台数据库为准。
- 卡内受密钥保护扇区保存平台随机生成的逻辑卡号，不能仅使用 UID 作为账户身份。
- 设备、读卡器与平台联网后才允许扣费和出水。
- 出水控制复用当前 Q74 和 ACK 机制。

## 整体架构

```mermaid
flowchart LR
    C[用户 M1 卡] --> R[读卡器]
    R --> F[GD32F303 控制板]
    F -->|MQTT 刷卡事件\n设备SN 卡UID 逻辑卡号 随机数| I[iot-service]
    I --> W[water-service]
    W --> D[(平台数据库\n卡账户 订单 余额流水)]
    W -->|授权成功| I
    I -->|Q74 出水命令| F
    F -->|Q74 ACK / 出水结果| I
    I --> W
    W --> D
```

## 发卡与绑定路径

```mermaid
flowchart TD
    A[管理员新增 IC 卡] --> B[平台生成随机逻辑卡号]
    B --> C[选择/创建客户余额账户]
    C --> D[读卡器写入逻辑卡号至 M1 受保护扇区]
    D --> E{扇区密钥认证成功?}
    E -->|否| F[发卡失败，不保存绑定]
    E -->|是| G[平台保存卡号、UID、客户、状态、密钥版本]
    G --> H[卡状态: 已启用]
```

## 用户刷卡取水主流程

```mermaid
sequenceDiagram
    autonumber
    participant U as 用户
    participant R as M1读卡器
    participant F as 控制板
    participant P as 平台
    participant DB as 卡账户/订单数据库

    U->>R: 刷卡
    R->>F: UID + 受保护扇区中的逻辑卡号
    F->>F: 扇区密钥认证，生成随机数
    F->>P: MQTT刷卡请求(设备SN、逻辑卡号、UID、随机数、时间戳)
    P->>P: 校验设备在线、卡状态、重复交易、限额
    P->>DB: 查询卡账户余额与水价
    alt 可售水
        P->>DB: 创建订单并冻结/预扣金额
        P->>F: MQTT授权结果(订单号、目标水量、冷热类型、签名、有效期)
        P->>F: Q74 出水命令
        F->>P: Q74 ACK
        F->>P: 实际出水结果/水量
        P->>DB: 完成订单，确认扣款或按实际量结算
    else 不可售水
        P->>F: 拒绝原因(离线、余额不足、卡冻结、超限)
        F->>U: 屏幕/语音提示
    end
```

## 异常与退款路径

```mermaid
flowchart TD
    A[已预扣的 IC 卡订单] --> B{Q74 ACK 是否成功?}
    B -->|否| C[订单异常]
    B -->|是| D{出水结果/实际水量正常?}
    D -->|是| E[完成订单，确认扣款]
    D -->|否| C
    C --> F[自动任务或实时处理]
    F --> G[退回卡账户余额]
    G --> H[记录退款流水、异常原因与审计日志]
    H --> I[设备界面显示失败/已退款]
```

## 分阶段实施路线

```mermaid
flowchart LR
    P1[阶段1: 硬件联调\nM1读卡、扇区认证、卡号上报] -->
    P2[阶段2: 平台卡管理\n发卡、绑定、挂失、卡状态] -->
    P3[阶段3: 卡账户\n充值、余额、流水、限额] -->
    P4[阶段4: 刷卡取水\n订单、预扣、Q74、ACK、结算] -->
    P5[阶段5: 运营保障\n自动退款、对账、报表、告警]
```

## 平台新增模块清单

| 模块 | 核心能力 |
| --- | --- |
| IC 卡管理 | 发卡、绑卡、挂失、冻结、注销、补卡、读卡记录 |
| 卡账户 | 余额、充值、冻结金额、扣款、退款、交易流水 |
| IC 卡订单 | 设备、卡号、冷热水、水量、金额、Q74 状态、出水状态、退款状态 |
| 风控规则 | 设备必须在线、单次/单日限额、短时间重复刷卡拦截、黑名单 |
| 财务报表 | 充值金额、售水金额、退款金额、卡余额、设备/日期维度统计 |
| 控制板协议 | 刷卡事件、授权响应、Q74、ACK、实际出水结果 |

## 控制板需准备的数据

| 上行刷卡事件字段 | 说明 |
| --- | --- |
| `deviceSn` | 当前控制板 SN，用于 MQTT 路由和设备映射 |
| `cardUid` | 卡物理 UID，仅用于辅助核验和风险识别 |
| `cardNo` | 受保护扇区读取的逻辑卡号，作为后台账户主查询键 |
| `nonce` | 单次随机数，防止报文重放 |
| `timestamp` | 设备产生事件的时间 |
| `readerStatus` | 认证成功、读卡失败、扇区异常等状态 |

## 正式上线前的最低安全要求

1. 不采用默认 M1 密钥，发卡时设置项目专用密钥并保密。
2. 不使用 UID 作为唯一账户身份；使用随机逻辑卡号。
3. 卡、账户、订单和余额流水的状态变更必须由平台事务处理。
4. 所有刷卡扣费必须经过在线平台校验；断网时默认拒绝售水。
5. 同一逻辑卡号短时间内的重复刷卡必须幂等，不能重复扣费或重复出水。
6. 先完成现有 Q66 心跳点位冲突和 messageID 幂等修复，再接入扣费出水链路。
